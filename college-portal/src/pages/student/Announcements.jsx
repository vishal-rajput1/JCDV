import { useEffect, useState } from 'react'
import { apiFetch } from '../../utils/api'
function Announcements() { const [items, setItems] = useState([]); const [error, setError] = useState(''); useEffect(() => { apiFetch('/student/announcements').then(setItems).catch((requestError) => setError(requestError.message)) }, []); return <div><p className="text-sm font-semibold text-brand-600">COMMUNICATION</p><h1 className="mt-1 text-3xl font-bold tracking-tight text-ink">Announcements</h1><p className="mt-2 text-sm text-muted">Published notices for your academic group.</p>{error ? <div className="mt-5 rounded-xl border border-rose-200 bg-rose-50 p-4 text-sm text-rose-700">{error}</div> : <div className="mt-8 space-y-4">{items.length ? items.map((item) => <article key={item.id} className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><p className="text-xs font-semibold text-brand-600">{item.publishDate}{item.subject ? ` · ${item.subject}` : ''}</p><h2 className="mt-2 text-lg font-bold text-ink">{item.title}</h2><p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-slate-600">{item.message}</p></article>) : <div className="rounded-2xl border border-dashed border-slate-300 bg-white p-12 text-center text-sm text-muted">No announcements are available.</div>}</div>}</div> }
export default Announcements
